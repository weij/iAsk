%li.question_count{style:"float:left; font-size:18px; font-weight:thin; color:#6996CF; padding:5px 0 0 10px;"}
  = @questions.count 
  questions
  
-if current_group.languages.count > 1
  %li{style: "float:right"}
    %label#lang-select-toggle{"data-language"=>change_language_filter_path}
      = render "shared/language_filter"  

%div{style: "float:right"}
  %label
    = t('global.sort_by')   
  -["newest", "votes", "activity", "hot", "answers"].each do |tab|  
    %li{class: tab}
      = pjax_link_to "#{t("questions.index.#{tab}")}#{triangle_arrow(tab, '▾')}", "index", url_for(params.merge(questions_link).merge(:sort => tab)), :title => t("questions.index.#{tab}_tooltip"), :rel => "nofollow" 

      






