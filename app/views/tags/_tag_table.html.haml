%table.tag-list
  = tag_table.each_slice(4) do |tags|
    %tr
      - tags.each do |tag|
        %td
          %div.tag{:class => "tag_#{tag.name}"}
            = tag_link(tag)
          %div.tag-number
            x
            = tag.count.to_i
#additional_info
  = paginate @tags
